#include <bits/stdc++.h>
#include <iostream>
using namespace std;
using ll = long long;

int subsets

    int
    main(int argc, char *argv[]) {
  int N, K;
  cin >> N >> K;

  cout << subsets(N, K);
  return 0;
}
